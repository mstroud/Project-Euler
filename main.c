#include "ProjectEuler.h"

int main(int argc, char *argv[])
{
    /* Usage will ProjectEuler N, where N is the problem number to run */
    /* Just for now I'm going to do this as a sanity check...          */
    PE1main();
    PE2main();
    return 0;
}
